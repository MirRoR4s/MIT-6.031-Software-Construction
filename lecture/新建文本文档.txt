本周任务：继续漏洞挖掘系统的后端开发。

1. 开发了针对staic、block、bytes字段的测试用例，共计200行代码。
2. 新增设置static、block、bytes字段，完成了api、controller、service三层开发，共计代码300行。
3. 利用 fastapi 的依赖注入和字段校验功能改进代码，具体包括新增全局依赖函数、参数校验等。
4. 利用 fastapi 请求体模型封装参数，使得设置原语函数更清晰明了。

